# Multilingual--Language-Translator
This project is a simple and interactive language translation web app built using Python and Streamlit. It allows users to enter text, automatically detect the source language and translate it into a selected target language. The app supports manual language selection,  swap languages feature and keeps a history of recent translations for reference
